# React Native Application Template

[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg?style=flat-square)](LICENSE)

A mobile app project template built with [React Native](https://reactnative.dev/)
and [Expo](https://expo.dev/), using the latest React Native / TypeScript
practices. It is the React Native member of nventive's application-template
family, built on a layered architecture with MVVM state management.

- It follows the **MVVM** principle, with an idiomatic React shape
  (RxJS observables + hooks instead of ViewModel classes).
- Code is organized by [application layer](doc/Architecture.md) — Access,
  Business, and Presentation — and by feature within each layer.
- It comes with a lightweight [dependency-injection](doc/DependencyInjection.md)
  composition root (no container).
- There are built-in [logs](doc/Logging.md) and [diagnostic tools](doc/Diagnostics.md).
- It ships the cross-cutting concerns a production app needs out of the box:
  environments, HTTP, storage, serialization, navigation, localization,
  theming, forced update, kill switch, remote config, analytics, crash
  reporting, and more (see [Documentation](#documentation)).
- There is scaffolding code showing a full sample feature. When you run it
  as-is, you get a _Dad Jokes_ application.

## Requirements

| All platforms      | Android                                    | iOS (macOS only)                    |
| ------------------ | ------------------------------------------ | ----------------------------------- |
| Node 22 LTS        | Android Studio (SDK 35+, an AVD/emulator)  | Xcode 26+ (Swift 6.2)               |
| Yarn 1 (classic)   | JDK 17                                     | CocoaPods                           |

This template uses **Expo with Continuous Native Generation**: the `android/`
and `ios/` projects are generated by `expo prebuild` and are **gitignored —
never hand-edit or commit them**. There is no EAS Build and no Expo Go
dependency; the app runs as a dev-client debug build produced by Gradle / Xcode.

See [doc/GettingStarted.md](doc/GettingStarted.md) for the full setup and
day-to-day workflow.

## Getting Started

### Run the template as-is

```sh
yarn install          # install dependencies
yarn android          # build & run on Android (expo run:android)
yarn ios              # build & run on iOS (macOS only, expo run:ios)
```

`expo run:android` / `expo run:ios` performs the prebuild automatically, builds
the debug app, installs it on the emulator/simulator, and starts Metro. Once a
dev-client build is installed, `yarn start` alone is enough for JavaScript
changes.

### Create your app from the template

After checking out the template, rename it into your own application with the
built-in generator:

```sh
yarn install
yarn generate --name "Acme App" --bundle-id com.acme.acmeapp
```

This substitutes the app name, slug, and bundle identifier / Android package
throughout the project, rewrites this README into a starter README, and removes
the template-only files (governance files and the generator itself). Add
`--dry-run` to preview. See [doc/ProjectGenerator.md](doc/ProjectGenerator.md)
for all options.

### Next steps

1. Rename the project with the generator (above).
2. Open [app.config.ts](app.config.ts) to review the app identity and
   per-environment configuration.
3. Copy the **Dad Jokes** feature (`src/{access,business,presentation}/jokes/`)
   as the pattern for your first feature — see
   [doc/DadJokes.md](doc/DadJokes.md).

## Architecture

The app is split into layers, with dependencies pointing strictly downward and
code organized **by feature within each layer**:

| Layer            | Folder              | Contains                                                        | May depend on                    |
| ---------------- | ------------------- | -------------------------------------------------------------- | -------------------------------- |
| **Access** (DAL) | `src/access/`       | HTTP clients, storage, native wrappers, DTOs (zod-parsed)      | nothing above it                 |
| **Business**     | `src/business/`     | Domain services & immutable entities (plain TS, RxJS)          | Access interfaces                |
| **Presentation** | `src/presentation/` | Screens, hooks, navigation, theme                              | Business interfaces (`useServices()`) |
| **Framework**    | `src/framework/`    | Composition root, providers, i18n, logging setup               | all (it wires them)              |

State reaches the UI through two explicit paths: **fetched** request/response
data via [React Query](doc/HTTP.md) (keys from a central factory), and **live**
domain state via an RxJS `BehaviorSubject` behind a service interface, consumed
through the single `useObservable` bridge. The full picture — including how state
management follows MVVM — is in [doc/Architecture.md](doc/Architecture.md).

## Documentation

Every topic has a recipe under the [doc/](doc/) folder. Start with
[doc/README.md](doc/README.md) for the complete index.

| Topic                                              | Implementation                                                   |
| -------------------------------------------------- | ---------------------------------------------------------------- |
| [Getting Started](doc/GettingStarted.md)           | Expo (Continuous Native Generation), Gradle / Xcode              |
| [Architecture](doc/Architecture.md)                | Layered (Access / Business / Presentation), MVVM                 |
| [Dependency Injection](doc/DependencyInjection.md) | Composition root + `ServicesProvider` / `useServices()`          |
| [Navigation](doc/Navigation.md)                    | [React Navigation](https://reactnavigation.org/) (typed routes)  |
| [Environments](doc/Environment.md)                 | `EnvironmentService` (MMKV + `expo-constants`, restart-to-apply) |
| [Design System](doc/DesignSystem.md)               | Theme tokens + base components, light/dark                       |
| [HTTP](doc/HTTP.md)                                 | [axios](https://axios-http.com/) + interceptors                  |
| [Async Data Loading](doc/Architecture.md)          | [TanStack Query](https://tanstack.com/query) + `queryKeys`       |
| [Local Storage](doc/LocalStorage.md)               | [MMKV](https://github.com/mrousavy/react-native-mmkv) + `expo-secure-store` |
| [Serialization](doc/Serialization.md)              | [zod](https://zod.dev/) DTO schemas                              |
| [Logging](doc/Logging.md)                          | `Logger` interface + console/file transports                     |
| [Diagnostics](doc/Diagnostics.md)                  | In-app overlay (env picker, mocking, log viewer)                 |
| [Error Handling](doc/ErrorHandling.md)             | Typed error taxonomy + `QueryStateView` + error boundary         |
| [Localization](doc/Localization.md)                | [i18next](https://www.i18next.com/) + `expo-localization`, en/fr |
| [Forced Update](doc/ForcedUpdate.md)               | Observable gate (`AppGate`) vs. remote minimum version           |
| [Kill Switch](doc/KillSwitch.md)                   | Observable gate (`AppGate`), auto-recovering                     |
| [Remote Config](doc/RemoteConfig.md)               | `RemoteConfigProvider` seam (mock / static)                      |
| [Firebase Remote Config](doc/FirebaseRemoteConfig.md) | Opt-in Firebase provider behind the seam                      |
| [Analytics](doc/Analytics.md)                      | `AnalyticsSink` seam (logging no-op by default)                  |
| [Crash Reporting](doc/CrashReporting.md)           | `CrashReporter` seam (opt-in Bugsee, internal builds)            |
| [Validation / Forms](doc/Forms.md)                 | [react-hook-form](https://react-hook-form.com/) + zod resolver   |
| [App Reviews](doc/AppReviews.md)                   | `expo-store-review` behind a policy service                      |
| [Testing](doc/Testing.md)                          | [Jest](https://jestjs.io/) + [RNTL](https://callstack.github.io/react-native-testing-library/) + [MSW](https://mswjs.io/) + [Maestro](https://maestro.mobile.dev/) |
| [Security Scan](doc/SecurityScan.md)               | Dependency (SCA) audit, nightly and non-blocking                 |
| [CI Pipeline](doc/AzurePipelines.md)               | [Azure Pipelines](https://azure.microsoft.com/products/devops/pipelines) (prebuild + Gradle/Xcode) |
| [Project Generator](doc/ProjectGenerator.md)       | In-place rename CLI (`yarn generate`)                            |
| [Dad Jokes sample](doc/DadJokes.md)                | Full vertical slice through every layer                          |

## Project structure

```
src/
  access/        Access layer — API clients, storage, native wrappers, DTOs
  business/      Business layer — domain services & entities
  presentation/  Presentation layer — screens, hooks, navigation, theme
  framework/     Composition root, providers, i18n, logging setup
  app/           App entry point
doc/             Per-concern documentation (this index)
test/            Cross-cutting & integration test suites
e2e/             Maestro end-to-end smoke flows
build/           Azure Pipelines definition & templates
cli/             Project-rename generator
```

## Quality gates

Run the verify loop before proposing any change; it is the same sequence CI runs
on every PR:

```sh
yarn typecheck        # TypeScript, no emit
yarn lint             # ESLint + Prettier
yarn test             # Jest unit & component tests
```

Additional scripts: `yarn test:coverage` (coverage report), `yarn e2e`
(Maestro smoke flows, device/CI only), and `yarn audit:scan` (dependency audit).
See [doc/Testing.md](doc/Testing.md).

## Breaking Changes

Please consult [BREAKING_CHANGES.md](BREAKING_CHANGES.md) for more information
about version history and compatibility.

## License

This project is licensed under the Apache 2.0 license — see the
[LICENSE](LICENSE) file for details.

## Contributing

Please read [CONTRIBUTING.md](CONTRIBUTING.md) for details on the process for
contributing to this project.

Be mindful of our [Code of Conduct](CODE_OF_CONDUCT.md).
